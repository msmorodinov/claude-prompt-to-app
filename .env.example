# No environment variables required for mock mode.
#
# For real Claude mode, ensure:
# - Claude Code CLI is installed and authenticated (claude.ai Max subscription)
# - Do NOT set ANTHROPIC_API_KEY (it overrides Max subscription OAuth)
#
# Optional:
# BACKEND_PORT=8000
# FRONTEND_PORT=5173
